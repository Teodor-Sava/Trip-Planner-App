import { FlexWrapDirective } from './flex-wrap.directive';

describe('FlexWrapDirective', () => {
  it('should create an instance', () => {
    const directive = new FlexWrapDirective();
    expect(directive).toBeTruthy();
  });
});
