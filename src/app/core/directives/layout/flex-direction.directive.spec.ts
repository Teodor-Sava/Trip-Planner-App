import { FlexDirectionDirective } from './flex-direction.directive';

describe('FlexDirectionDirective', () => {
  it('should create an instance', () => {
    const directive = new FlexDirectionDirective();
    expect(directive).toBeTruthy();
  });
});
