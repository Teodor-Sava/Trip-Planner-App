import { FlexAlignDirective } from './flex-align.directive';

describe('FlexAlignDirective', () => {
  it('should create an instance', () => {
    const directive = new FlexAlignDirective();
    expect(directive).toBeTruthy();
  });
});
