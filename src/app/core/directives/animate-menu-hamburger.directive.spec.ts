import { AnimateMenuHamburgerDirective } from './animate-menu-hamburger.directive';

describe('AnimateMenuHamburgerDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimateMenuHamburgerDirective();
    expect(directive).toBeTruthy();
  });
});
